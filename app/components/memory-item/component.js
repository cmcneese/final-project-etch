import Ember from 'ember';

export default Ember.Component.extend({
  tagName: '',

  // didReceiveAttrs() {
  //   console.log(this.get('memory.track.name'));
  //
  //   this.set('track', this.get('memory.track'));
  //   this.set('album', this.get('memory.track.album.images.firstObject'));
  // },

});
