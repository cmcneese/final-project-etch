import Ember from 'ember';
// import Memory from 'app/models/memory';

export default Ember.Route.extend({
  // spotify: Ember.inject.service('spotify-api'),

//   var attributes = Ember.get(Memory, 'attributes')
//
//   attributes.forEach(model(trackId, user) {
// });

  // model(params) {
  //   // TODO: use spotify service to fetch tracks for each memory
  //   // return this.get('spotify').tracks(params.search);
  //
  //   // return this.store.query('memory', { user: params.user_id });
  // }
});
