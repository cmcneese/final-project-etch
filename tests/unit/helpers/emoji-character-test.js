
import { emojiCharacter } from 'final-project-etch/helpers/emoji-character';
import { module, test } from 'qunit';

module('Unit | Helper | emoji character');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = emojiCharacter([42]);
  assert.ok(result);
});

